# Co-op Cinema Configuration

# Server address (host:port) — takes priority if set
# SERVER_ADDR=:8080

# Port only (used by Render, Railway, Fly.io)
# If SERVER_ADDR is not set, falls back to PORT, then defaults to 8080
PORT=8080
